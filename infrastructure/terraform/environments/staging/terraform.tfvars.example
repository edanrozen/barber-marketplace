# Copy to terraform.tfvars (gitignored) and fill per environment. NO SECRETS here —
# secrets live in AWS Secrets Manager (see modules/secrets). This file holds only
# non-secret, environment-specific values.
aws_region = "eu-central-1"
