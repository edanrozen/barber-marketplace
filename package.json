{
  "name": "barber-marketplace",
  "version": "0.0.0",
  "private": true,
  "description": "At-home barber marketplace — Israel-first, Hebrew/RTL. Modular monolith, TypeScript everywhere.",
  "workspaces": [
    "apps/*",
    "packages/*",
    "backend"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "prepare": "husky",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0"
  },
  "//": "devDependencies are the lint/format/hook toolchain mandated by the Engineering Constitution (§IV, §XII). Version ranges are conservative and are resolved/locked by the lockfile on `npm install` in the networked dev environment. backend/modules/* are FOLDERS, not packages."
}
